name: 'Painel da coordenação'
description: |-

  B8, segunda esticada do desafio 5.1: agregados ANÔNIMOS pra quem coordena
  — nunca aluno nomeado (ver docs/dominio/PAINEL_COORDENACAO.md). `insights`
  não tem tabela própria: tudo aqui vem de `AcademicStatsProvider`, contrato
  do `core` implementado no `journey`.

  O escopo (curso × instituição) é resolvido AQUI, a partir do usuário
  autenticado — nunca de um `course_id` que o cliente mande. Coordenador
  enxerga o próprio curso (`usr.course_crs_id`); `institution_admin` enxerga
  a instituição inteira (`courseId: null`, o `EntityScope` já faz o resto).
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/staff/insights/bottlenecks
    metadata:
      custom: []
      groupName: 'Painel da coordenação'
      groupDescription: |-

        B8, segunda esticada do desafio 5.1: agregados ANÔNIMOS pra quem coordena
        — nunca aluno nomeado (ver docs/dominio/PAINEL_COORDENACAO.md). `insights`
        não tem tabela própria: tudo aqui vem de `AcademicStatsProvider`, contrato
        do `core` implementado no `journey`.

        O escopo (curso × instituição) é resolvido AQUI, a partir do usuário
        autenticado — nunca de um `course_id` que o cliente mande. Coordenador
        enxerga o próprio curso (`usr.course_crs_id`); `institution_admin` enxerga
        a instituição inteira (`courseId: null`, o `EntityScope` já faz o resto).
      subgroup: ''
      subgroupDescription: ''
      title: 'Onde a turma empaca'
      description: |-
        Taxa de reprovação por disciplina, nota e falta separadas, nos últimos
        `last_terms` termos com matrícula. Disciplinas com menos de 5
        tentativas resolvidas não aparecem (piso de anonimato).
      authenticated: true
      deprecated: false
    headers:
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      last_terms:
        custom: []
        name: last_terms
        description: 'Quantos termos recentes considerar. Default: 4.'
        required: false
        example: 4
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      last_terms: 4
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"subject_code":"MAT029","subject_name":"Cálculo 2","total_attempts":40,"failed_grade":15,"failed_absence":6,"failed_both":0,"failure_rate":0.525}]}'
        headers: []
        description: 'com gargalo'
    responseFields: []
    auth: []
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/staff/insights/cohorts
    metadata:
      custom: []
      groupName: 'Painel da coordenação'
      groupDescription: |-

        B8, segunda esticada do desafio 5.1: agregados ANÔNIMOS pra quem coordena
        — nunca aluno nomeado (ver docs/dominio/PAINEL_COORDENACAO.md). `insights`
        não tem tabela própria: tudo aqui vem de `AcademicStatsProvider`, contrato
        do `core` implementado no `journey`.

        O escopo (curso × instituição) é resolvido AQUI, a partir do usuário
        autenticado — nunca de um `course_id` que o cliente mande. Coordenador
        enxerga o próprio curso (`usr.course_crs_id`); `institution_admin` enxerga
        a instituição inteira (`courseId: null`, o `EntityScope` já faz o resto).
      subgroup: ''
      subgroupDescription: ''
      title: 'Quanto custa'
      description: |-
        Atraso médio (em termos além do previsto pela matriz) entre os
        vínculos ativos de cada coorte de ingresso. Coortes com menos de 5
        vínculos ativos não aparecem (piso de anonimato).
      authenticated: true
      deprecated: false
    headers:
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"entry_term":"2022/1","active_registrations":38,"avg_delay_terms":2.4}]}'
        headers: []
        description: 'com atraso'
    responseFields: []
    auth: []
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/staff/insights/at-risk
    metadata:
      custom: []
      groupName: 'Painel da coordenação'
      groupDescription: |-

        B8, segunda esticada do desafio 5.1: agregados ANÔNIMOS pra quem coordena
        — nunca aluno nomeado (ver docs/dominio/PAINEL_COORDENACAO.md). `insights`
        não tem tabela própria: tudo aqui vem de `AcademicStatsProvider`, contrato
        do `core` implementado no `journey`.

        O escopo (curso × instituição) é resolvido AQUI, a partir do usuário
        autenticado — nunca de um `course_id` que o cliente mande. Coordenador
        enxerga o próprio curso (`usr.course_crs_id`); `institution_admin` enxerga
        a instituição inteira (`courseId: null`, o `EntityScope` já faz o resto).
      subgroup: ''
      subgroupDescription: ''
      title: 'Quem está perto do limite'
      description: |-
        Quantidade de vínculos ativos cuja previsão de formatura cabe em
        `within` termos ou menos até o prazo de integralização.
      authenticated: true
      deprecated: false
    headers:
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      within:
        custom: []
        name: within
        description: 'Quantos termos até o prazo contam como "perto". Default: 2.'
        required: false
        example: 2
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      within: 2
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"count":12,"within_terms":2}}'
        headers: []
        description: ok
    responseFields: []
    auth: []
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/staff/insights/demand
    metadata:
      custom: []
      groupName: 'Painel da coordenação'
      groupDescription: |-

        B8, segunda esticada do desafio 5.1: agregados ANÔNIMOS pra quem coordena
        — nunca aluno nomeado (ver docs/dominio/PAINEL_COORDENACAO.md). `insights`
        não tem tabela própria: tudo aqui vem de `AcademicStatsProvider`, contrato
        do `core` implementado no `journey`.

        O escopo (curso × instituição) é resolvido AQUI, a partir do usuário
        autenticado — nunca de um `course_id` que o cliente mande. Coordenador
        enxerga o próprio curso (`usr.course_crs_id`); `institution_admin` enxerga
        a instituição inteira (`courseId: null`, o `EntityScope` já faz o resto).
      subgroup: ''
      subgroupDescription: ''
      title: 'O que está represado'
      description: |-
        Quantos vínculos ativos já estão elegíveis para cada disciplina ainda
        não cursada — planejamento de oferta direto, sem chute. Disciplinas
        com menos de 5 elegíveis não aparecem (piso de anonimato).
      authenticated: true
      deprecated: false
    headers:
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"subject_code":"ES52C","subject_name":"Engenharia de Software II","demand":22}]}'
        headers: []
        description: 'com demanda'
    responseFields: []
    auth: []
    controller: null
    method: null
    route: null
